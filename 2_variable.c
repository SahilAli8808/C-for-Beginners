// varriable is container which is store the value
// for e.g a=4 here a is assigned to 4
#include <stdio.h>

int main()
{
    int a = 4;
    int b = 5.5;
    float d = 8.5;
    char c = 'a';
    printf("the value of a,b is %d and %d\n", a, b);
    printf("the value of d, c is %f and %c\n", d, c);
    printf("the sum of a and b is %d:\n", a + b);
    return 0;
}